{
  "name": "nmedia",
  "version": "1.0.0",
  "description": "Media processing service for images, video, and audio",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "node tests/index.js",
    "test:e2e": "node tests/e2e-runner.js",
    "test:e2e:manual": "echo 'Start service with npm start, then run: node tests/e2e.test.js'",
    "postinstall": "npm install --prefix modules/nImage && npm install --prefix modules/nVideo && npm install --prefix modules/nLogger"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
